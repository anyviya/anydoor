1.匹配模式前/代表项目根目录
2.匹配模式最后加/代表是目录
3.匹配模式前加!代表取反

应用情景
比如我们的需求是，要忽略整个文件夹，但是保留其中一个文件不被忽略
一上来就忽略整个文件夹，然后在这个单个文件前面写个叹号，该文件就不会被忽略了


*代表任意字符(这个和javascript类似)
？匹配任意一个字符
**匹配多级目录
#代表注释

任意级目录
node_modules/**/index.js